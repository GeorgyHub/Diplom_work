import matplotlib.pyplot as plt

x = ['Новая', 'В работе', 'Ожидание', 'Выполнена', 'Закрыта']
y = [18, 27, 8, 40, 26]

plt.bar(x, y)
plt.xlabel('Статус')
plt.ylabel('Количество заявок')
plt.title('Распределение заявок по статусам')
plt.show()